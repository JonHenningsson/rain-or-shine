{
  "name": "rain-or-shine",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/JonHenningsson/rain-or-shine",
  "author": "Jon Henningsson <jon.henningsson@gmail.com>",
  "license": "MIT",
  "scripts": {
    "devbuild": "yarn run devenv && yarn run hugo --gc --minify",
    "build": "yarn run prodenv && hugo --gc --minify",
    "start": "yarn run devenv && yarn run hugo server",
    "prodenv": "export HUGO_PARAMS_STRAVA_REDIRECTBASEURL=${URL} HUGO_PARAMS_STRAVA_CID={STRAVA_API_CLIENT_ID}",
    "devenv": "export HUGO_PARAMS_STRAVA_CID={STRAVA_API_CLIENT_ID}"
  },
  "dependencies": {
    "mystrava": "file:functions/common/mystrava/",
    "myuserdb": "file:functions/common/myuserdb/"
  },
  "devDependencies": {
    "hugo-bin": "^0.58.0",
    "netlify-cli": "^2.50.0"
  }
}
